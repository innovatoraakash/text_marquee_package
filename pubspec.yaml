name: scrolling_text
description: This package is a Text ticker that helps to make vertical or horizontal scrolling text with custom callback, speed and style .
version: 0.0.2
homepage: https://github.com/innovatoraakash/text_marquee_package

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0




flutter:


